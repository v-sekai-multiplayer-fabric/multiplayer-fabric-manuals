# V-Sekai Multiplayer Fabric Deck Log — 2026-06-06 (Saturday)

Date: 2026-06-06

## Added

- Decision: [Compiling the Godot engine](../../decisions/20260606-compiling-godot-engine.md) — local build SOP using `sccache` as the compiler launcher with a shared cache on `E:`, and a `gscons` function for both PowerShell and WSL.
- Decision (MADR): [Two-org split between V-Sekai-fire and v-sekai-multiplayer-fabric](../../decisions/20260606-org-split-v-sekai-multiplayer-fabric.md) — records the partial migration and the old→new repo mapping.
- Decision (MADR): [Require pull requests and a merge queue on main](../../decisions/20260606-require-pr-and-merge-queue-on-main.md).
- `index.md` capabilities table mapping native video playback, OpenUSD scene baking, spatial audio, speech, and pen-stroke creation (codename cassie, `feat/module-cassie`) to their engine branches and supporting repos. Pen stroke creation works; patch surface creation loses about 90%.
- Repository ruleset on `main` (id 17352485): require a pull request plus a squash merge queue; block deletion and non-fast-forward. Repo `allow_auto_merge` enabled so PRs can enter the queue.

## Changed

- Rewrote the `index.md` repository map for the live `v-sekai-multiplayer-fabric` org, grouped by role (engine, runtime, Harvester VM images, rendering/USD, spatial, tooling, infra, archived), with a cross-org "Still on V-Sekai-fire" section for repos that have not migrated.
- `AGENTS.md` adopts the MADR template for new decisions.
- `_quarto.yml` site and repo URLs point at `v-sekai-multiplayer-fabric`.
