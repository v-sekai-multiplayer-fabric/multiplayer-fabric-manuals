<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>V-Sekai Fire — Manuals</title>
<link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions.html</link>
<atom:link href="https://v-sekai-multiplayer-fabric.github.io/manuals/decisions.xml" rel="self" type="application/rss+xml"/>
<description></description>
<generator>quarto-1.9.38</generator>
<lastBuildDate>Sat, 06 Jun 2026 00:00:00 GMT</lastBuildDate>
<item>
  <title>Spatial audio via a patched Resonance Audio (HRTF and audio probes)</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-spatial-audio-patched-resonance-audio.html</link>
  <description><![CDATA[ 





<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The zone server renders shared social-VR scenes where sound sources need to be localized in 3D for every listener. Two things drive presence: binaural HRTF rendering so a source is heard at the correct azimuth and elevation over headphones, and audio probes that capture how a room colors and occludes sound so a source baked behind a wall does not leak through it. Godot’s built-in <code>AudioServer</code> does stereo panning and attenuation, but it has no HRTF path and no probe-based spatialization. How should the engine produce HRTF spatial audio with environmental probes?</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>Binaural HRTF output for headphone VR.</li>
<li>Audio probes for environmental response and occlusion, bakeable per scene.</li>
<li>Double-precision engine build; the audio module has to link into the fork.</li>
<li>Reuse a proven spatialization library rather than writing one.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Godot built-in <code>AudioServer</code> panning only.</li>
<li>Steam Audio (Valve) integration.</li>
<li>A patched Resonance Audio integrated as an engine module plus a spatial-audio server.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: “A patched Resonance Audio integrated as an engine module”, because it provides HRTF binaural rendering and ambisonic room response with an open-source base the fork can carry, and it slots into the engine as a module rather than a runtime GDExtension.</p>
<p>The implementation lives on two engine feature branches:</p>
<ul>
<li><code>feat/module-resonance-audio</code> — the patched Resonance Audio vendored as an engine module, exposing HRTF binaural rendering and audio probes.</li>
<li><code>feat/spatial-audio-server</code> — the spatial-audio server that drives the module, placing sources and listeners and resolving probes per frame.</li>
</ul>
<p><a href="https://github.com/v-sekai-multiplayer-fabric/sponza-godot-audio">sponza-godot-audio</a> is the demo and benchmark scene.</p>
<section id="consequences" class="level3">
<h3 class="anchored" data-anchor-id="consequences">Consequences</h3>
<ul>
<li>Good: headphone listeners get correct HRTF localization, and probes give rooms a consistent response with occlusion.</li>
<li>Good: the module links into the double-precision fork and ships with the engine assembly.</li>
<li>Neutral: the fork carries a patch set against Resonance Audio, but the rebase burden is low. Upstream is abandoned (Google archived the project), so there are no upstream changes to track, and the engine is pinned to a frozen Godot 4.7 commit, so the module does not chase engine movement. The fork owns the code.</li>
<li>Bad: HRTF and probe resolution add per-frame audio cost that the spatial-audio server has to budget.</li>
</ul>
</section>
<section id="confirmation" class="level3">
<h3 class="anchored" data-anchor-id="confirmation">Confirmation</h3>
<p>The two branches assemble into the engine and the <code>sponza-godot-audio</code> scene renders HRTF output with probes. The capability is tracked in the <a href="../index.html#capabilities-and-where-they-live">index capabilities table</a>.</p>
</section>
</section>
<section id="more-information" class="level2">
<h2 class="anchored" data-anchor-id="more-information">More Information</h2>
<p>Resonance Audio is Google’s open-source spatial audio SDK (<a href="https://resonance-audio.github.io/resonance-audio/" class="uri">https://resonance-audio.github.io/resonance-audio/</a>), now archived and no longer maintained upstream, so the fork is the de facto source. “Patched” here means the fork carries local changes to build it as an engine module and to wire its HRTF and probe paths to the spatial-audio server. The engine itself is pinned to a frozen Godot 4.7 commit, so the module targets a fixed engine API.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-spatial-audio-patched-resonance-audio.html</guid>
  <pubDate>Sat, 06 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Require pull requests and a merge queue on main</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-require-pr-and-merge-queue-on-main.html</link>
  <description><![CDATA[ 





<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The <code>manuals</code> repo had no branch protection on <code>main</code>. Changes landed by pushing directly or by manually merging pull requests one at a time, with no serialization between concurrent PRs. As the number of in-flight documentation PRs grows, two PRs can each pass against an older <code>main</code> and then conflict or break the Quarto build once both land. How should changes reach <code>main</code> so that every merge is reviewable and tested against the tip it will actually join?</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>Every change to <code>main</code> should arrive through a pull request, not a direct push.</li>
<li>Concurrent PRs should serialize so each is validated against the latest <code>main</code>.</li>
<li>The team is small, so the process should add little ceremony (no mandatory second reviewer).</li>
<li>The setting should be declarative and recorded, not click-ops folklore.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Leave <code>main</code> unprotected (status quo).</li>
<li>Branch protection requiring a pull request only.</li>
<li>A repository ruleset requiring a pull request plus a merge queue.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: “A repository ruleset requiring a pull request plus a merge queue”, because it serializes merges and keeps <code>main</code> PR-only without forcing a second reviewer.</p>
<p>The ruleset targets the default branch with these rules:</p>
<ul>
<li>Block branch deletion and non-fast-forward pushes.</li>
<li>Require a pull request, with <code>0</code> required approvals (review is allowed, not mandated) and all three merge methods permitted on the PR.</li>
<li>Require a merge queue using the squash merge method, <code>ALLGREEN</code> grouping, up to 5 entries built and merged per batch, and a 1-minute minimum wait.</li>
</ul>
<section id="consequences" class="level3">
<h3 class="anchored" data-anchor-id="consequences">Consequences</h3>
<ul>
<li>Good: <code>main</code> only changes through PRs, and the queue tests each entry against the branch tip before it lands.</li>
<li>Good: the configuration is captured here and reproducible from the ruleset JSON.</li>
<li>Bad: solo edits now need a PR and an enqueue step.</li>
<li>Bad: with <code>0</code> required approvals, the rule enforces process but not review, so an unreviewed PR can still merge.</li>
</ul>
</section>
<section id="confirmation" class="level3">
<h3 class="anchored" data-anchor-id="confirmation">Confirmation</h3>
<p>The ruleset is active (id <code>17352485</code>). <code>gh api repos/v-sekai-multiplayer-fabric/manuals/rulesets</code> lists it, and a direct push to <code>main</code> is rejected. Future PRs land via the queue.</p>
</section>
</section>
<section id="more-information" class="level2">
<h2 class="anchored" data-anchor-id="more-information">More Information</h2>
<p>Created with <code>gh api -X POST repos/v-sekai-multiplayer-fabric/manuals/rulesets</code>. To change the policy, edit the ruleset rather than protecting the branch through the classic branch-protection API, so the two mechanisms do not overlap.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-require-pr-and-merge-queue-on-main.html</guid>
  <pubDate>Sat, 06 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Presence demo networking internals</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-presence-demo-networking-internals.html</link>
  <description><![CDATA[ 





<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The <a href="../decisions/20260606-multiplayer-presence-demo-orbs.html">presence demo</a> vetoes Godot’s high-level networking and sends pose data with low-level explicit calls. This records the engine pieces that make that work and how a head or hand pose reaches the wire. The relevant code lives on the engine fork’s feature branches, which the <a href="https://github.com/v-sekai-multiplayer-fabric/merge"><code>merge</code></a> recipe assembles; the engine is <a href="../decisions/20260606-pin-engine-to-frozen-godot-4-7.html">pinned to a frozen Godot 4.7 commit</a>.</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>High-rate pose streams must be compact, so the wire format has to be tight.</li>
<li>Reuse the engine’s own WebTransport stack rather than a side channel.</li>
<li>Avoid the per-property bookkeeping of automatic scene replication.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>High-level scene replication (<code>MultiplayerSynchronizer</code> / <code>MultiplayerSpawner</code>).</li>
<li>Explicit RPC over a standard <code>MultiplayerPeer</code> (ENet / WebRTC / WebSocket).</li>
<li>Explicit messages over the engine’s WebTransport (HTTP/3) module.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: “Explicit messages over the engine’s WebTransport module”, with poses encoded as compact binary. High-level replication stays off.</p>
<section id="transport-the-http3-webtransport-module" class="level3">
<h3 class="anchored" data-anchor-id="transport-the-http3-webtransport-module">Transport: the HTTP/3 / WebTransport module</h3>
<p>On the <code>feat/module-http3</code> branch, <code>modules/http3</code> provides the WebTransport stack:</p>
<ul>
<li><code>modules/http3/quic_picoquic_backend.{cpp,h}</code> — native QUIC via picoquic.</li>
<li><code>modules/http3/quic_web_backend.cpp</code> + <code>quic_web_glue.js</code> — the web/wasm backend.</li>
<li><code>modules/http3/http3_client.{cpp,h}</code>, <code>quic_client.{cpp,h}</code>, <code>quic_server.h</code>.</li>
<li>Exposed classes (<code>modules/http3/doc_classes/</code>): <code>HTTP3Client</code>, <code>QUICClient</code>, <code>QUICServer</code>, <code>WebTransportPeer</code>.</li>
<li>Demos: <code>modules/http3/demo/wt_client_test.gd</code>, <code>wt_server_demo.gd</code>, <code>wt_browser_test.html</code>.</li>
<li><code>lean/http3/PollingTermination.lean</code> carries a Lean proof about the polling loop.</li>
</ul>
<p>WebTransport gives reliable streams and unreliable datagrams over one QUIC connection, so high-rate pose updates ride datagrams.</p>
</section>
<section id="pose-source" class="level3">
<h3 class="anchored" data-anchor-id="pose-source">Pose source</h3>
<p>Poses come from the XR trackers (<code>servers/xr/</code>):</p>
<ul>
<li><code>XRPositionalTracker</code> (<code>servers/xr/xr_positional_tracker.h</code>) holds named <code>XRPose</code> entries with a hand enum.</li>
<li><code>XRPose</code> (<code>servers/xr/xr_pose.h</code>) carries a <code>Transform3D</code>, linear and angular velocity, and a tracking-confidence level.</li>
<li><code>XRHandTracker</code> (<code>servers/xr/xr_hand_tracker.h</code>) tracks 26 hand joints, each a <code>Transform3D</code>.</li>
</ul>
<p>The demo reads the local head and hand poses, encodes them, and sends them; remote peers apply the decoded transforms to orb nodes.</p>
</section>
<section id="what-is-vetoed-and-why" class="level3">
<h3 class="anchored" data-anchor-id="what-is-vetoed-and-why">What is vetoed and why</h3>
<p><code>modules/multiplayer</code> ships the high-level nodes (<code>MultiplayerSynchronizer</code>, <code>MultiplayerSpawner</code>, <code>SceneReplicationInterface</code>) and the Variant-based RPC path (<code>MultiplayerAPI::encode_and_compress_variants</code>). Variant encoding of a <code>Transform3D</code> is general but fat, so it is not used for the pose stream. The demo hand-rolls a compact binary encoding instead. Standard <code>MultiplayerPeer</code> transports (<code>modules/enet</code>, <code>modules/webrtc</code>, <code>modules/websocket</code>) exist, but the stack’s transport is WebTransport.</p>
</section>
<section id="identity-stretch" class="level3">
<h3 class="anchored" data-anchor-id="identity-stretch">Identity (stretch)</h3>
<p>The pen, <code>modules/cassie</code> on <code>feat/module-cassie</code>, supplies the optional <a href="../decisions/20260606-feature-classification-poc-baseline-stretch.html">signing identity</a>.</p>
</section>
<section id="consequences" class="level3">
<h3 class="anchored" data-anchor-id="consequences">Consequences</h3>
<ul>
<li>Good: poses ride compact datagrams over the same WebTransport the gateway uses.</li>
<li>Good: the engine already proves the transport with the <code>http3</code> demos and a Lean termination proof.</li>
<li>Bad: the compact encoding, send rate, and interpolation are hand-written and have to be maintained.</li>
<li>Bad: the WebTransport, cassie, and fabric modules live on separate feature branches, so the full stack only exists in the assembled engine.</li>
</ul>
</section>
<section id="confirmation" class="level3">
<h3 class="anchored" data-anchor-id="confirmation">Confirmation</h3>
<p>The <code>modules/http3</code> demos open a WebTransport client and server. The presence demo sends head and hand poses as datagrams and a remote peer renders the orbs from the decoded transforms.</p>
</section>
</section>
<section id="more-information" class="level2">
<h2 class="anchored" data-anchor-id="more-information">More Information</h2>
<p>Modules cited here are on the engine fork’s feature branches (<code>feat/module-http3</code>, <code>feat/module-cassie</code>) and are composed by the <code>merge</code> <code>gitassembly</code> recipe onto the frozen Godot 4.7 base.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-presence-demo-networking-internals.html</guid>
  <pubDate>Sat, 06 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Pin the engine to a frozen Godot 4.7 commit</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-pin-engine-to-frozen-godot-4-7.html</link>
  <description><![CDATA[ 





<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The engine fork carries many feature branches (cassie, resonance audio, native media, speech, spatial audio, the fabric modules) that the <code>merge</code> recipe assembles onto a base. If that base tracks a moving upstream, every assembly can shift under the patches, so a green build one day can break the next from upstream churn alone. What base should the feature branches and the assembly build on?</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>A stable base so assembly and CI are reproducible.</li>
<li>Patch branches that do not have to chase upstream API changes mid-cycle.</li>
<li>A known engine version for the docs and for downstream images.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Track upstream <code>godotengine/godot</code> <code>master</code>.</li>
<li>Track an upstream release branch.</li>
<li>Pin the fork’s <code>master</code> to one frozen upstream Godot 4.7 commit.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: “Pin the fork’s <code>master</code> to one frozen upstream Godot 4.7 commit”, because it gives the patch branches and the assembly a fixed target, so builds are reproducible and upstream churn cannot break an assembly.</p>
<ul>
<li>The fork’s <code>master</code> is the frozen base. Its tip is <code>8a337510</code> (Godot <code>4.7.0-beta</code>, per <code>version.py</code>).</li>
<li>Every feature branch in the <a href="https://github.com/v-sekai-multiplayer-fabric/merge"><code>merge</code></a> <code>gitassembly</code> recipe stands alone on <code>master</code>; the recipe merges them onto the assembled branch from that base.</li>
<li>The pin moves only by a deliberate update to <code>master</code>, not by following upstream.</li>
</ul>
<section id="consequences" class="level3">
<h3 class="anchored" data-anchor-id="consequences">Consequences</h3>
<ul>
<li>Good: assemblies and CI are reproducible against a fixed engine.</li>
<li>Good: feature branches target one fixed engine API.</li>
<li>Bad: upstream fixes after the pin are not picked up until <code>master</code> is advanced on purpose.</li>
<li>Bad: the longer the pin sits on a beta, the larger the eventual catch-up to a later 4.7.</li>
</ul>
</section>
<section id="confirmation" class="level3">
<h3 class="anchored" data-anchor-id="confirmation">Confirmation</h3>
<p><code>version.py</code> on the fork reports <code>4.7.0-beta</code>, and the <code>gitassembly</code> recipe bases its branches on <code>master</code>. Advancing the engine is a single, reviewable change to the <code>master</code> pin.</p>
</section>
</section>
<section id="more-information" class="level2">
<h2 class="anchored" data-anchor-id="more-information">More Information</h2>
<p>This pin is why the <a href="../decisions/20260606-spatial-audio-patched-resonance-audio.html">spatial audio decision</a> notes the module targets a fixed engine API. The exact upstream commit that <code>master</code> mirrors lives in the godot fork’s git history.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-pin-engine-to-frozen-godot-4-7.html</guid>
  <pubDate>Sat, 06 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Two-org split between V-Sekai-fire and v-sekai-multiplayer-fabric</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-org-split-v-sekai-multiplayer-fabric.html</link>
  <description><![CDATA[ 





<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The repository map in <code>index.md</code> described a single org, <code>V-Sekai-fire</code>, built around a <code>multiplayer-fabric</code> submodule umbrella whose repos were named <code>multiplayer-fabric-*</code>. The stack has since grown a second GitHub org, <a href="https://github.com/v-sekai-multiplayer-fabric"><code>v-sekai-multiplayer-fabric</code></a>, which now hosts the engine, the Harvester VM images, the rendering and USD research, and these manuals. The two orgs use different repo names, and the old repos do not redirect into the new org. The documentation map had drifted far enough from reality that a reader could not find the live repos.</p>
<p>How should the manuals represent the stack while both orgs are in active use?</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>The map has to point at repos that actually resolve.</li>
<li>Historical decision records and changelog entries link to <code>V-Sekai-fire</code> repos that still exist; those links must keep working.</li>
<li>New work happens in <code>v-sekai-multiplayer-fabric</code>, so that org should read as the primary home.</li>
<li>The migration is partial. Several services have not moved.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Rewrite every <code>V-Sekai-fire</code> reference to <code>v-sekai-multiplayer-fabric</code>.</li>
<li>Document <code>v-sekai-multiplayer-fabric</code> as the home org and keep a marked cross-org section for the repos still under <code>V-Sekai-fire</code>.</li>
<li>Leave the map untouched and add a disclaimer.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: “Document <code>v-sekai-multiplayer-fabric</code> as the home org and keep a marked cross-org section”, because it gives readers an accurate map of the live org without breaking the historical record.</p>
<p>Concretely:</p>
<ul>
<li><code>index.md</code> lists the <code>v-sekai-multiplayer-fabric</code> repos grouped by role, then a “Still on V-Sekai-fire” section for the repos that have not migrated.</li>
<li>Living docs (<code>index.md</code>, <code>_quarto.yml</code>, <code>AGENTS.md</code>) name the new org.</li>
<li>Decision records and changelog entries keep their <code>V-Sekai-fire</code> links, which still resolve.</li>
</ul>
<section id="migration-status" class="level3">
<h3 class="anchored" data-anchor-id="migration-status">Migration status</h3>
<table class="caption-top table">
<colgroup>
<col style="width: 17%">
<col style="width: 45%">
<col style="width: 37%">
</colgroup>
<thead>
<tr class="header">
<th>Role</th>
<th>V-Sekai-fire (old)</th>
<th>v-sekai-multiplayer-fabric (new)</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Engine</td>
<td>multiplayer-fabric-godot</td>
<td>godot</td>
</tr>
<tr class="even">
<td>Engine merge</td>
<td>multiplayer-fabric-merge</td>
<td>merge</td>
</tr>
<tr class="odd">
<td>Engine build</td>
<td>multiplayer-fabric-build</td>
<td>godot-images</td>
</tr>
<tr class="even">
<td>Zone server</td>
<td>multiplayer-fabric-zone</td>
<td>zone-server (+ zone-server-image)</td>
</tr>
<tr class="odd">
<td>Zone backend</td>
<td>multiplayer-fabric-zone-backend</td>
<td>zone-backend (+ zone-backend-image)</td>
</tr>
<tr class="even">
<td>Baker</td>
<td>multiplayer-fabric-baker</td>
<td>zone-baker (+ zone-baker-image)</td>
</tr>
<tr class="odd">
<td>Observability</td>
<td>multiplayer-fabric-observability</td>
<td>observability</td>
</tr>
<tr class="even">
<td>Database</td>
<td>multiplayer-fabric-crdb</td>
<td>cockroach-crdb-image</td>
</tr>
<tr class="odd">
<td>Infra</td>
<td>multiplayer-fabric-infra</td>
<td>infra (Harvester HCI)</td>
</tr>
<tr class="even">
<td>Spatial proofs</td>
<td>multiplayer-fabric-predictive-bvh-research</td>
<td>lean-predictive-bvh</td>
</tr>
<tr class="odd">
<td>Sandbox programs</td>
<td>multiplayer-fabric-elf-programs</td>
<td>godot-sandbox-programs</td>
</tr>
</tbody>
</table>
<p>Not yet migrated, still under V-Sekai-fire: gateway, zone-console, webtransport, taskweft, humanoid-project, aria-storage, elixir-turboquant-llm, cycle-tests, and the <code>multiplayer-fabric</code> umbrella.</p>
</section>
<section id="consequences" class="level3">
<h3 class="anchored" data-anchor-id="consequences">Consequences</h3>
<ul>
<li>Good: every repo link in the map resolves, and new contributors land in the right org.</li>
<li>Good: the historical record stays intact and citable.</li>
<li>Bad: the stack is described across two orgs until migration finishes, so the map needs upkeep as repos move.</li>
</ul>
</section>
<section id="confirmation" class="level3">
<h3 class="anchored" data-anchor-id="confirmation">Confirmation</h3>
<p>A link check over <code>index.md</code> resolves all repo URLs. The “Still on V-Sekai-fire” section shrinks as repos migrate; each move updates the table above.</p>
</section>
</section>
<section id="more-information" class="level2">
<h2 class="anchored" data-anchor-id="more-information">More Information</h2>
<p>The deployment target is also shifting, from Fly.io to a self-hosted Harvester HCI cluster running each service as a podman-quadlet VM image. See the <code>infra</code> repo’s <code>docs/migration-status.md</code> for that progress.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-org-split-v-sekai-multiplayer-fabric.html</guid>
  <pubDate>Sat, 06 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Minimal multiplayer presence demo with head and hand orbs</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-multiplayer-presence-demo-orbs.html</link>
  <description><![CDATA[ 





<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The <code>xr-grid</code> debug scene currently shows one skinned-mesh hero asset (a rigged mesh with a skeleton, not a VRM) standing on the grid as a visual landmark; it does not represent a player. Around it float a few clusters of small colored orbs. Each cluster encodes the translation and orientation (a full pose) of one tracked point — head, hand, hand — for a remote participant, drawn as orientation and translation orbs. The next increment should prove shared presence: several people in the same space at once, at the scale of a full V-Sekai Discord call. What is the smallest piece that demonstrates real multiplayer presence without committing to full networked avatars for everyone?</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/attachments/20260606_vsekai-mpf_xr-grid-debug-orbs_0001.png" class="img-fluid figure-img"></p>
<figcaption>The <code>xr-grid</code> debug scene, 2026-06-06: a skinned-mesh hero asset as a visual landmark, with each remote participant drawn as head and hand orientation-and-translation orb clusters. <span class="citation" data-cites="vsekai_xrgrid_debug_2026">(V-Sekai Multiplayer Fabric 2026)</span></figcaption>
</figure>
</div>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>Smallest demonstrable multiplayer increment on top of the existing scene.</li>
<li>Real presence for many participants, roughly a Discord call’s worth.</li>
<li>Cheap per-player representation that still reads as a person.</li>
<li>Identity should be optional, not a login wall.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Full networked character (skinned mesh) for every participant.</li>
<li>Head plus two hand orbs per participant (3-point tracking representation).</li>
<li>Text nameplates with no embodiment.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: “Head plus two hand orbs per participant”, because three tracked points per person convey presence and gesture at a fraction of the cost of a full avatar, which lets the demo scale to a whole call.</p>
<p>Scope of the demo:</p>
<ul>
<li>Every participant, including the local player, is represented by three tracked points — head, hand, hand — each shown as a cluster of orbs that encodes its translation and orientation (pose). The existing skinned-mesh hero asset stays in the scene as a visual landmark and does not represent any player.</li>
<li>The room scales to a V-Sekai Discord call’s worth of participants.</li>
<li>A participant who wants to identify themselves signs with the pen (<a href="../decisions/20260606-feature-classification-poc-baseline-stretch.html">cassie</a>); the signature is the identity mark. Unsigned participants stay anonymous orbs.</li>
<li>Built on <a href="https://github.com/v-sekai-multiplayer-fabric/xr-grid">xr-grid</a>.</li>
</ul>
<section id="networking" class="level3">
<h3 class="anchored" data-anchor-id="networking">Networking</h3>
<p>Godot’s high-level networking is vetoed for this demo. Automatic scene replication (the <code>MultiplayerSpawner</code> and <code>MultiplayerSynchronizer</code> nodes and the <code>SceneMultiplayer</code> replicator) is not used. Orb poses go over low-level, explicit RPC calls instead, so the demo controls exactly what is sent each tick and how it is encoded. See the <a href="../decisions/20260606-presence-demo-networking-internals.html">networking technical details</a>.</p>
</section>
<section id="consequences" class="level3">
<h3 class="anchored" data-anchor-id="consequences">Consequences</h3>
<ul>
<li>Good: presence and gesture for many people at low per-player cost.</li>
<li>Good: identity is opt-in through signing, so there is no login step.</li>
<li>Bad: orbs are not full avatars, so the demo does not exercise avatar networking or IK.</li>
<li>Neutral: pen signing depends on cassie, whose pen stroke creation is solid, so signing (which only needs strokes) is reliable. Only cassie’s patch surface creation is buggy (loses about 90%), and signing does not depend on that.</li>
<li>Neutral: explicit RPC means hand-rolling the encoding, send rate, and interpolation. That cost is the point. Pose streams have to be fast and succinct, so they need a compact binary wire format. If it could be easy it would be JSON, and JSON is too fat for high-rate pose updates.</li>
</ul>
</section>
<section id="confirmation" class="level3">
<h3 class="anchored" data-anchor-id="confirmation">Confirmation</h3>
<p>A session shows multiple participants, each rendered as three orb clusters (head, hand, hand) tracking the pose of those points, at call scale. Signing with the pen produces a visible identifying mark next to that participant’s orbs.</p>
</section>
</section>
<section id="more-information" class="level2">
<h2 class="anchored" data-anchor-id="more-information">More Information</h2>
<p>This is the “smallest new piece” milestone: expand the existing <code>xr-grid</code> debug scene (a skinned-mesh hero asset as a landmark, plus orientation and translation orb clusters) into a populated room. It composes the <a href="../decisions/20260606-feature-classification-poc-baseline-stretch.html">pen-stroke / cassie capability</a> for optional identity.</p>



</section>

<div id="quarto-appendix" class="default"><section class="quarto-appendix-contents" id="quarto-bibliography"><h2 class="anchored quarto-appendix-heading">References</h2><div id="refs" class="references csl-bib-body hanging-indent">
<div id="ref-vsekai_xrgrid_debug_2026" class="csl-entry">
V-Sekai Multiplayer Fabric. 2026. <em><span class="nocase">xr-grid</span> Debug Scene: Hero-Asset Landmark with Head and Hand Orientation-and-Translation Orbs</em>.
</div>
</div></section></div> ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-multiplayer-presence-demo-orbs.html</guid>
  <pubDate>Sat, 06 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Feature classification — proof of concept, baseline, stretch</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-feature-classification-poc-baseline-stretch.html</link>
  <description><![CDATA[ 





<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The capabilities table lists features with a free-text status (“working”, “loses about 90%”). Free text does not say whether a feature is a throwaway experiment, a committed part of the product, or a nice-to-have. The team needs one shared vocabulary for how far along and how committed a feature is, so planning and the docs agree on what “done” means for each one.</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>One vocabulary shared between planning and the manuals.</li>
<li>A reader should know at a glance whether a feature is committed or exploratory.</li>
<li>Cheap to apply and to move as a feature matures.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Keep free-text status only.</li>
<li>A maturity ladder (alpha / beta / stable).</li>
<li>A three-tier commitment classification: proof of concept, baseline, stretch.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: “A three-tier commitment classification”, because it captures commitment, not just maturity, which is what planning needs.</p>
<p>The tiers:</p>
<ol type="1">
<li><strong>Proof of concept</strong> — demonstrates the idea works end to end, in partial or throwaway form. May be unreliable or lossy, and nothing depends on it yet.</li>
<li><strong>Baseline</strong> — the committed minimum the product ships. Works reliably across the target platforms. This is the bar.</li>
<li><strong>Stretch goal</strong> — beyond baseline. Pursued if time allows; cutting it does not block the release.</li>
</ol>
<p>Each feature in the <a href="../index.html#capabilities-and-where-they-live">capabilities table</a> carries one tier. A feature moves between tiers as it matures or as commitment changes; the tier is the current call, not a permanent label.</p>
<section id="initial-triage" class="level3">
<h3 class="anchored" data-anchor-id="initial-triage">Initial triage</h3>
<ul>
<li>Native video playback — baseline.</li>
<li>Scene baking via OpenUSD — baseline.</li>
<li>Spatial audio (HRTF + audio probes) — baseline.</li>
<li>Speech — baseline.</li>
<li>Pen stroke creation (cassie) — proof of concept (patch surface creation loses about 90%).</li>
</ul>
</section>
<section id="consequences" class="level3">
<h3 class="anchored" data-anchor-id="consequences">Consequences</h3>
<ul>
<li>Good: planning and docs share one word per feature for its commitment level.</li>
<li>Good: the tier is a small, reversible edit as features move.</li>
<li>Bad: a tier is a judgement call and can drift from reality if the table is not kept current.</li>
</ul>
</section>
<section id="confirmation" class="level3">
<h3 class="anchored" data-anchor-id="confirmation">Confirmation</h3>
<p>The capabilities table has a Tier column, and every row carries one of the three tiers. New capabilities are added with a tier.</p>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-feature-classification-poc-baseline-stretch.html</guid>
  <pubDate>Sat, 06 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Compiling the Godot engine</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-compiling-godot-engine.html</link>
  <description><![CDATA[ 





<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p>Developers build the V-Sekai multiplayer-fabric Godot engine on two hosts from one workstation: <strong>Windows (PowerShell + MinGW)</strong> and <strong>WSL / Linux (<code>linuxbsd</code>)</strong>. Without a shared, persistent compiler cache, each host rebuilds from scratch and cache hits are lost whenever a checkout is moved or renamed.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Standardise on <a href="https://github.com/mozilla/sccache"><code>sccache</code></a> as the compiler launcher plus SCons’s own <code>CacheDir</code>, both pointed at a single physical directory on the <code>E:</code> drive so Windows and WSL share one store. Wrap the build in a <code>gscons</code> shell function on each host.</p>
<section id="prerequisites" class="level3">
<h3 class="anchored" data-anchor-id="prerequisites">Prerequisites</h3>
<table class="caption-top table">
<colgroup>
<col style="width: 11%">
<col style="width: 43%">
<col style="width: 44%">
</colgroup>
<thead>
<tr class="header">
<th>Tool</th>
<th>Windows</th>
<th>WSL (Ubuntu 24.04)</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Python 3</td>
<td><code>scoop install python</code></td>
<td>system <code>python3</code></td>
</tr>
<tr class="even">
<td>SCons</td>
<td><code>python -m pip install scons</code></td>
<td><code>python3 -m pip install scons</code></td>
</tr>
<tr class="odd">
<td>Compiler</td>
<td>MinGW-w64 (<code>use_mingw=yes</code>)</td>
<td><code>build-essential</code> (gcc/g++)</td>
</tr>
<tr class="even">
<td>sccache</td>
<td><code>scoop install sccache</code></td>
<td><code>brew install sccache</code></td>
</tr>
<tr class="odd">
<td>Git</td>
<td><code>scoop install git</code></td>
<td>system <code>git</code></td>
</tr>
</tbody>
</table>
<p>Verify <code>sccache --version</code> resolves in each shell. The verified setup uses <strong>sccache 0.15.0</strong>.</p>
</section>
<section id="shared-cache-layout" class="level3">
<h3 class="anchored" data-anchor-id="shared-cache-layout">Shared cache layout</h3>
<p>WSL mounts <code>E:</code> at <code>/mnt/e</code>, so the same files back both build hosts:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 21%">
<col style="width: 36%">
<col style="width: 41%">
</colgroup>
<thead>
<tr class="header">
<th>Purpose</th>
<th>Windows path</th>
<th>WSL path</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>sccache object cache</td>
<td><code>E:\godot-build-cache\sccache</code></td>
<td><code>/mnt/e/godot-build-cache/sccache</code></td>
</tr>
<tr class="even">
<td>SCons <code>CacheDir</code></td>
<td><code>E:\godot-build-cache\scons_cache</code></td>
<td><code>/mnt/e/godot-build-cache/scons_cache</code></td>
</tr>
</tbody>
</table>
<p><code>sccache</code> keys include the compiler, target triple, and flags, so MinGW and Linux objects coexist in the same store without colliding — they simply do not hit each other. Sharing the location keeps cache management to one directory.</p>
<p><code>SCCACHE_BASEDIRS</code> (the equivalent of ccache’s <code>CCACHE_BASEDIR</code>) strips a leading absolute prefix before hashing so hits survive a moved/renamed checkout. Set it to the checkout root. Paths must be <strong>absolute</strong>; separate multiple dirs with <code>;</code> on Windows and <code>:</code> elsewhere (longest matching prefix wins). The server reads it at startup — run <code>sccache --stop-server</code> after changing it.</p>
</section>
<section id="windows-powershell-profile" class="level3">
<h3 class="anchored" data-anchor-id="windows-powershell-profile">Windows — PowerShell profile</h3>
<p>Add to <code>Documents\WindowsPowerShell\Microsoft.PowerShell_profile.ps1</code>:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode powershell code-with-copy"><code class="sourceCode powershell"><span id="cb1-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Shared build cache on E: — same physical dir as WSL (/mnt/e/godot-build-cache).</span></span>
<span id="cb1-2"><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$env</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">SCCACHE_DIR</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"E:\godot-build-cache\sccache"</span></span>
<span id="cb1-3"><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$env</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">SCCACHE_CACHE_SIZE</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"20G"</span></span>
<span id="cb1-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Strip the checkout root from compile paths so cache hits survive a moved/renamed build dir.</span></span>
<span id="cb1-5"><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$env</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">SCCACHE_BASEDIRS</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"E:\godot"</span></span>
<span id="cb1-6"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">New-Item</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>ItemType Directory <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>Force <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>Path <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$env</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">SCCACHE_DIR</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">,</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"E:\godot-build-cache\scons_cache"</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">|</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">Out-Null</span></span>
<span id="cb1-7"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">function</span> gscons <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span> python <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>m SCons platform<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>windows use_mingw<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>yes compiledb<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>yes target<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>editor precision<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>double c_compiler_launcher<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>sccache cpp_compiler_launcher<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>sccache cache_path<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>E<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:/</span>godot-build-cache<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>scons_cache <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>j16 @args <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span></span></code></pre></div></div>
</section>
<section id="wsl-linux-.bashrc" class="level3">
<h3 class="anchored" data-anchor-id="wsl-linux-.bashrc">WSL / Linux — <code>~/.bashrc</code></h3>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb2-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Godot V-Sekai linuxbsd build.</span></span>
<span id="cb2-2"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Uses sccache as the compiler launcher to cache object compilation.</span></span>
<span id="cb2-3"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">export</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">SCCACHE_DIR</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">${SCCACHE_DIR</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:-</span>/mnt/e/godot-build-cache/sccache<span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">}</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span></span>
<span id="cb2-4"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">export</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">SCCACHE_CACHE_SIZE</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">${SCCACHE_CACHE_SIZE</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:-</span>20G<span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">}</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span></span>
<span id="cb2-5"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Strip the checkout root from compile paths so cache hits survive a moved/renamed build dir.</span></span>
<span id="cb2-6"><span class="bu" style="color: null;
background-color: null;
font-style: inherit;">export</span> <span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">SCCACHE_BASEDIRS</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">${SCCACHE_BASEDIRS</span><span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:-</span>/mnt/e/godot<span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">}</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span></span>
<span id="cb2-7"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">gscons()</span> <span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">{</span></span>
<span id="cb2-8">    <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">python3</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-m</span> SCons compiledb=yes target=editor precision=double <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">\</span></span>
<span id="cb2-9">        c_compiler_launcher=sccache cpp_compiler_launcher=sccache <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">\</span></span>
<span id="cb2-10">        cache_path=/mnt/e/godot-build-cache/scons_cache debug_symbols=yes tests=yes <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-j</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$(</span><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">nproc</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">)</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$@</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span></span>
<span id="cb2-11"><span class="kw" style="color: #003B4F;
background-color: null;
font-weight: bold;
font-style: inherit;">}</span></span></code></pre></div></div>
<p>Open a fresh shell (or <code>source ~/.bashrc</code> / <code>. $PROFILE</code>) so <code>gscons</code> is defined.</p>
</section>
<section id="building" class="level3">
<h3 class="anchored" data-anchor-id="building">Building</h3>
<p>From the engine checkout (<code>E:\godot</code> / <code>/mnt/e/godot</code>):</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb3-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">gscons</span>                      <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># full editor build</span></span>
<span id="cb3-2"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">gscons</span> verbose=yes          <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># extra args pass straight through to SCons</span></span></code></pre></div></div>
<p>Output binary:</p>
<ul>
<li>Windows: <code>bin\godot.windows.editor.double.x86_64.exe</code></li>
<li>WSL: <code>bin/godot.linuxbsd.editor.double.x86_64</code></li>
</ul>
<p>The two functions differ only where the platform requires it: Windows pins <code>platform=windows use_mingw=yes</code> and <code>-j16</code>; WSL infers <code>platform=linuxbsd</code>, uses <code>-j$(nproc)</code>, and adds <code>debug_symbols=yes tests=yes</code>. Both share <code>compiledb=yes target=editor precision=double</code> and the sccache launchers.</p>
</section>
<section id="verifying-and-maintaining-the-cache" class="level3">
<h3 class="anchored" data-anchor-id="verifying-and-maintaining-the-cache">Verifying and maintaining the cache</h3>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode sh code-with-copy"><code class="sourceCode bash"><span id="cb4-1"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">sccache</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--show-stats</span>        <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># "Compile requests" / "Cache hits" climb across builds</span></span>
<span id="cb4-2"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">sccache</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--stop-server</span>       <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># apply changed SCCACHE_* env vars</span></span>
<span id="cb4-3"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">sccache</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--zero-stats</span>        <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># reset counters</span></span></code></pre></div></div>
<p>A clean first build is mostly misses; a second build of the same tree shows a high hit rate and finishes substantially faster. The store is capped at <code>SCCACHE_CACHE_SIZE</code> (20G) and evicts least-recently-used entries automatically.</p>
</section>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>One cache directory on <code>E:</code> serves both hosts; management is a single location.</li>
<li>The engine <code>SConstruct</code> recognises only <code>cache_path</code> and <code>cache_limit</code> for the SCons cache. <code>scons_cache=</code> is <strong>not</strong> valid and is silently ignored.</li>
<li>WSL builds run against <code>/mnt/e</code>, whose 9p mount is slower than native ext4 — the trade-off accepted for a shared cache.</li>
<li>Common failures: no hits between builds → server started before the env vars, run <code>sccache --stop-server</code>; no hits after relocating a checkout → set <code>SCCACHE_BASEDIRS</code> to the checkout root.</li>
</ul>
</section>
<section id="further-reading" class="level2">
<h2 class="anchored" data-anchor-id="further-reading">Further reading</h2>
<ul>
<li><a href="https://github.com/mozilla/sccache/blob/main/docs/Local.md">sccache local cache (<code>SCCACHE_DIR</code>, <code>SCCACHE_CACHE_SIZE</code>)</a></li>
<li><a href="https://github.com/mozilla/sccache/blob/main/docs/Configuration.md">sccache configuration (<code>SCCACHE_BASEDIRS</code>)</a></li>
<li><a href="https://github.com/godotengine/godot/blob/master/SConstruct">Godot <code>SConstruct</code></a></li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-compiling-godot-engine.html</guid>
  <pubDate>Sat, 06 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Recursive art-game loop and its minimal steel thread</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-art-game-loop-steel-thread.html</link>
  <description><![CDATA[ 





<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>We want a small workspace where friends make tiny art-games together, watch them, and improve them on a tight loop. Players show up in the space as tracker orbs (the <a href="../decisions/20260606-multiplayer-presence-demo-orbs.html">presence demo</a>), with drawing pens (<a href="../decisions/20260606-feature-classification-poc-baseline-stretch.html">cassie</a>) as a stretch goal. Before building the workspace out, we need the smallest end-to-end thread that proves the loop runs at all. What is that thread?</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>A tight, local-first feedback loop friends can actually turn.</li>
<li>Visible results each pass, so friction is obvious.</li>
<li>One friction note and one patch at a time, not a backlog.</li>
<li>The smallest path that touches every link from runtime to verified change.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Build the full tool-chain and content pipeline first.</li>
<li>A minimal steel thread that exercises every link once, then grow it.</li>
<li>Hand-run the loop with no tooling.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: “A minimal steel thread that exercises every link once”, because it proves the whole loop end to end before we invest in any one part.</p>
<section id="the-recursive-art-game-loop" class="level3">
<h3 class="anchored" data-anchor-id="the-recursive-art-game-loop">The recursive art-game loop</h3>
<ol type="1">
<li>Friends create a tiny art-game.</li>
<li>They play and observe it, capturing friction.</li>
<li>We patch the art-game or the tools.</li>
<li>We replay the improved version.</li>
<li>Repeat.</li>
</ol>
<p>Players appear as tracker orbs; drawing pens are a stretch goal layered on once the loop turns.</p>
</section>
<section id="the-minimal-steel-thread" class="level3">
<h3 class="anchored" data-anchor-id="the-minimal-steel-thread">The minimal steel thread</h3>
<ol type="1">
<li>Start a local Godot runtime/editor.</li>
<li>Connect <a href="https://github.com/v-sekai-multiplayer-fabric/vsekai-godot-mcp">Godot MCP</a>.</li>
<li>Run one <a href="https://github.com/v-sekai-multiplayer-fabric/godot-sandbox-programs">Godot Sandbox</a> command/program.</li>
<li>Create or modify one visible art object.</li>
<li>Record one friction note.</li>
<li>Patch one tiny behavior in the art-game or tools.</li>
<li>Re-run and verify the visible result changed.</li>
</ol>
<p>This builds on the <a href="https://github.com/v-sekai-multiplayer-fabric/friends-art-game-loop">friends-art-game-loop</a> workspace.</p>
</section>
<section id="consequences" class="level3">
<h3 class="anchored" data-anchor-id="consequences">Consequences</h3>
<ul>
<li>Good: every link from runtime to verified change is exercised once, so gaps surface early.</li>
<li>Good: the loop produces a visible change and one friction note per pass, which keeps scope honest.</li>
<li>Bad: the first thread is deliberately thin and does no real game design yet.</li>
<li>Bad: it leans on Godot MCP and the sandbox, so a break in either stalls the loop.</li>
</ul>
</section>
<section id="confirmation" class="level3">
<h3 class="anchored" data-anchor-id="confirmation">Confirmation</h3>
<p>The steel thread completes from a cold local editor through to a re-run that shows a changed visible result, with one friction note and one patch recorded.</p>
</section>
</section>
<section id="more-information" class="level2">
<h2 class="anchored" data-anchor-id="more-information">More Information</h2>
<p>This composes the <a href="../decisions/20260606-multiplayer-presence-demo-orbs.html">presence demo</a> (tracker orbs) and the cassie pen (stretch) into a working loop, run through Godot MCP and the Godot Sandbox.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-art-game-loop-steel-thread.html</guid>
  <pubDate>Sat, 06 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Archival file naming convention for committed assets</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-archival-file-naming-convention.html</link>
  <description><![CDATA[ 





<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The manuals repo now commits binary assets alongside prose, starting with screenshots under <code>decisions/attachments/</code>. Ad-hoc names like <code>image.png</code> collide, sort poorly, and lose their provenance once they leave the page that referenced them. What naming convention should committed assets follow so they stay unique, sortable, and self-describing?</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>Names should be unique and not collide across decisions.</li>
<li>Names should sort chronologically and read as self-describing.</li>
<li>Names should survive being copied out of the repo (provenance in the name).</li>
<li>The convention should match established digital-preservation guidance.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Keep source names (<code>image.png</code>, <code>screenshot (1).png</code>).</li>
<li>Content hash names (<code>a1b2c3.png</code>).</li>
<li>A Library of Congress style descriptive convention.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: “A Library of Congress style descriptive convention”, because it follows recognized preservation guidance and yields names that sort and explain themselves.</p>
<p>The pattern:</p>
<pre><code>YYYYMMDD_project_description_NNNN.ext</code></pre>
<p>Following the Library of Congress file-naming guidance:</p>
<ul>
<li>Lowercase ASCII only; no spaces or special characters.</li>
<li>ISO 8601 date first (<code>YYYYMMDD</code>) so names sort chronologically.</li>
<li>Facets separated by underscores (<code>_</code>); words within a facet by hyphens (<code>-</code>).</li>
<li>A zero-padded sequence (<code>NNNN</code>) to keep same-day captures unique and ordered.</li>
<li>Descriptive but reasonably short.</li>
</ul>
<p>Example, used for the first committed screenshot:</p>
<pre><code>20260606_vsekai-mpf_xr-grid-debug-orbs_0001.png</code></pre>
<p>Each committed asset also gets a BibTeX entry in <code>references.bib</code> recording its capture date and archived path, so the References page lists it.</p>
<section id="consequences" class="level3">
<h3 class="anchored" data-anchor-id="consequences">Consequences</h3>
<ul>
<li>Good: assets are unique, sort by date, and carry their provenance in the name.</li>
<li>Good: the convention matches preservation practice and is mechanical to apply.</li>
<li>Bad: names are longer than source names, and renaming on import is a manual step.</li>
</ul>
</section>
<section id="confirmation" class="level3">
<h3 class="anchored" data-anchor-id="confirmation">Confirmation</h3>
<p>Assets under <code>decisions/attachments/</code> match <code>YYYYMMDD_project_description_NNNN.ext</code> and have a matching <code>references.bib</code> entry. The first example is <code>20260606_vsekai-mpf_xr-grid-debug-orbs_0001.png</code>, cited from the <a href="../decisions/20260606-multiplayer-presence-demo-orbs.html">presence-demo decision</a>.</p>
</section>
</section>
<section id="more-information" class="level2">
<h2 class="anchored" data-anchor-id="more-information">More Information</h2>
<p>Based on the Library of Congress guidance on file naming for digital preservation (descriptive, no spaces, lowercase, ISO dates, zero-padded sequences).</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260606-archival-file-naming-convention.html</guid>
  <pubDate>Sat, 06 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Replace Jaeger with VictoriaTraces for trace storage</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260506-observability-stack-victoriatraces.html</link>
  <description><![CDATA[ 





<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p>Jaeger all-in-one with Badger storage was chosen as the Apache 2.0 replacement for Tempo. VictoriaMetrics ships VictoriaTraces — an Apache 2.0 trace backend that accepts OTLP directly. VictoriaMetrics benchmarks report 3.7× less RAM and 2.6× less CPU vs Tempo.</p>
<p>Using VictoriaTraces puts all three storage backends (metrics, logs, traces) under the same vendor, drops Badger’s single-node limit, and matches the port and CLI-flag conventions already in use.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Replace Jaeger all-in-one with VictoriaTraces in the single-machine observability stack.</p>
<ul>
<li>VictoriaTraces listens on 10428 (HTTP, UI + query + OTLP HTTP ingest).</li>
<li>OTEL Collector exports traces via <code>otlphttp/traces</code> to <code>http://localhost:10428/insert/opentelemetry</code>.</li>
<li>Data persists at <code>/var/lib/victoriatraces</code> on the shared Fly volume.</li>
<li>Jaeger is removed entirely; no separate query UI process is needed.</li>
</ul>
<p>Port map after this change:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 21%">
<col style="width: 33%">
<col style="width: 45%">
</colgroup>
<thead>
<tr class="header">
<th>Service</th>
<th>Port</th>
<th>Purpose</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>VictoriaMetrics</td>
<td>8428</td>
<td>Metrics storage and PromQL</td>
</tr>
<tr class="even">
<td>VictoriaLogs</td>
<td>9428</td>
<td>Log storage and query</td>
</tr>
<tr class="odd">
<td>VictoriaTraces</td>
<td>10428</td>
<td>Trace storage and query</td>
</tr>
<tr class="even">
<td>OTEL Collector</td>
<td>4317 (gRPC), 4318 (HTTP)</td>
<td>OTLP ingest, routes to the above</td>
</tr>
</tbody>
</table>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>All four services are now Apache 2.0; the three storage backends are from VictoriaMetrics.</li>
<li>VictoriaTraces query UI is at <code>http://...:10428/select/vmui</code>.</li>
<li><code>fly proxy 10428:10428</code> replaces the former <code>fly proxy 16686:16686</code> for trace inspection.</li>
<li>No explicit trace TTL is configured; add <code>-retentionPeriod</code> if disk pressure becomes a concern.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260506-observability-stack-victoriatraces.html</guid>
  <pubDate>Wed, 06 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Observability stack with VictoriaMetrics, VictoriaLogs, and Tempo</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260506-observability-stack-victoriametrics-tempo.html</link>
  <description><![CDATA[ 





<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p>The stack needs metrics, logs, and traces. Options considered: Grafana Cloud (cost), self-hosted Prometheus+Loki+Tempo (three separate apps), or a compact single-machine stack.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Run VictoriaMetrics (metrics), VictoriaLogs (logs), and Grafana Tempo (traces) under supervisord in a single Fly Machine, with an OpenTelemetry Collector as the ingest router.</p>
<ul>
<li>OTEL Collector listens on 4317 (gRPC) and 4318 (HTTP) on the private network.</li>
<li>Collector routes: metrics -&gt; VictoriaMetrics (8428 via Prometheus remote write), logs -&gt; VictoriaLogs (9428 OTLP), traces -&gt; Tempo (5317 OTLP internal).</li>
<li>Data persists on a 10 GB Fly volume.</li>
</ul>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>OTLP ports must not be exposed publicly — they accept unauthenticated writes.</li>
<li>Other Fly apps send telemetry to <code>multiplayer-fabric-observability.internal:4317</code> or <code>:4318</code>.</li>
<li>Grafana Tempo v3 config changed significantly from v2: <code>ingester</code> and <code>compactor</code> top-level keys were removed; <code>-target=all</code> flag is required to run all components.</li>
<li>See <a href="../decisions/20260506-ghcr-package-ownership-same-repo.html">GHCR package ownership decision</a> for why the zone binary image was renamed and moved to <code>multiplayer-fabric-zone</code>.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260506-observability-stack-victoriametrics-tempo.html</guid>
  <pubDate>Wed, 06 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Observability stack with VictoriaMetrics, VictoriaLogs, and Jaeger</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260506-observability-stack-victoriametrics-jaeger.html</link>
  <description><![CDATA[ 





<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p>Grafana Tempo was chosen for trace storage in the initial stack but is licensed under AGPL-3.0, which requires any service modifications to be open-sourced. Jaeger (Apache 2.0, CNCF graduated) fits the permissive-license constraint: it is OTLP-native since v1.35 and commonly deployed alongside VictoriaMetrics.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Replace Grafana Tempo with Jaeger all-in-one in the single-machine observability stack.</p>
<ul>
<li>Jaeger listens on 14317 (OTLP gRPC, internal) and exposes its UI on 16686.</li>
<li>OTEL Collector routes traces to <code>localhost:14317</code> instead of the former Tempo port (5317).</li>
<li>Jaeger uses Badger embedded storage (<code>/var/lib/jaeger/data</code> and <code>/var/lib/jaeger/keys</code>) persisted on the same 10 GB Fly volume.</li>
<li><code>tempo-config.yaml</code> is deleted; Jaeger is configured entirely via CLI flags in <code>supervisord.conf</code>.</li>
</ul>
<p>Port map after this change:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 21%">
<col style="width: 33%">
<col style="width: 45%">
</colgroup>
<thead>
<tr class="header">
<th>Service</th>
<th>Port</th>
<th>Purpose</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>VictoriaMetrics</td>
<td>8428</td>
<td>Metrics storage and PromQL</td>
</tr>
<tr class="even">
<td>VictoriaLogs</td>
<td>9428</td>
<td>Log storage and query</td>
</tr>
<tr class="odd">
<td>Jaeger UI</td>
<td>16686</td>
<td>Trace storage and query</td>
</tr>
<tr class="even">
<td>OTEL Collector</td>
<td>4317 (gRPC), 4318 (HTTP)</td>
<td>OTLP ingest, routes to the above</td>
</tr>
</tbody>
</table>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>Jaeger UI replaces the Tempo query endpoint: <code>fly proxy 16686:16686</code> instead of <code>fly proxy 3200:3200</code>.</li>
<li>Badger is ephemeral-safe only with <code>--badger.ephemeral=false</code>; this flag must be set or traces are lost on restart.</li>
<li>No explicit trace TTL is configured; traces accumulate until disk pressure. Add <code>--badger.span-store-ttl</code> if retention limits are needed.</li>
<li>Jaeger’s Badger backend does not support distributed mode; acceptable for a single-machine stack.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260506-observability-stack-victoriametrics-jaeger.html</guid>
  <pubDate>Wed, 06 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>GHCR packages must be built by the repo that consumes them</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260506-ghcr-package-ownership-same-repo.html</link>
  <description><![CDATA[ 





<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p>The zone server binary was built by <code>multiplayer-fabric-baker</code> and pushed to <code>ghcr.io/v-sekai-fire/godot-zone-double</code>. The zone deploy workflow (in <code>multiplayer-fabric-zone</code>) used <code>--local-only</code> with <code>docker/login-action</code> to pull that image, but received 403 Forbidden.</p>
<p>GitHub Container Registry ties package write access to the repository whose <code>GITHUB_TOKEN</code> created it. The zone repo’s token could not push to a package owned by the baker repo, and could not pull a private package owned by another repo without package-scoped access.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Each repository builds and publishes the GHCR images it directly uses in its own deploy:</p>
<ul>
<li><code>multiplayer-fabric-baker</code> builds and owns <code>godot-editor-double</code>.</li>
<li><code>multiplayer-fabric-zone</code> builds and owns <code>multiplayer-fabric-zone-godot</code> (renamed from <code>godot-zone-double</code> to reflect ownership).</li>
</ul>
<p><code>workflow_run</code> triggers only fire within the same repository, so the zone binary build workflow must live in the zone repo for the deploy to auto-trigger after a successful build.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>Package names must reflect the owning repo to avoid confusion.</li>
<li>Moving a package between repos requires deleting it (needs <code>delete:packages</code> API scope) and rebuilding, or renaming.</li>
<li>Cross-repo GHCR access requires either a PAT with <code>read:packages</code> scope or making the package public.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260506-ghcr-package-ownership-same-repo.html</guid>
  <pubDate>Wed, 06 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Use WebTransport over QUIC for game traffic</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260501-webtransport-over-quic-for-game-traffic.html</link>
  <description><![CDATA[ 





<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p>The zone server needs low-latency bidirectional communication between clients and the Godot game server. HTTP/1.1 and WebSocket both run over TCP, which head-of-line blocks on packet loss and degrades real-time game state.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Use WebTransport (HTTP/3 over QUIC) for all game traffic. Clients connect to the Elixir gateway on UDP port 443, which proxies into the Godot zone server on UDP port 7443.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>UDP eliminates TCP head-of-line blocking.</li>
<li>Fly.io passes UDP without DNAT, so the app must bind to the same port clients connect to.</li>
<li>Port 443 requires running as root (or CAP_NET_BIND_SERVICE). The gateway container runs as root.</li>
<li>Datagrams are used for game state messages. Streams are avoided for ping/pong to sidestep stream half-close deadlock (client must close write side before server response fires).</li>
<li>Cloudflare’s proxy cannot forward QUIC/UDP. All game-traffic DNS records must be DNS-only (no proxy).</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260501-webtransport-over-quic-for-game-traffic.html</guid>
  <pubDate>Fri, 01 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Godot double precision template_release for zone servers</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260501-godot-double-precision-template-release-for-zone.html</link>
  <description><![CDATA[ 





<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p>Zone servers run the game simulation headlessly. Two Godot build targets are candidates: <code>editor</code> (includes import tools) and <code>template_release</code> (runtime only). Precision must match the rest of the V-Sekai stack.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Build zone server binaries as <code>target=template_release precision=double</code> with no Mono. Build from the V-Sekai fork at <code>V-Sekai-fire/multiplayer-fabric-build@b27142e94</code>.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li><code>template_release</code> has no import or export tools, making it smaller and faster to start.</li>
<li><code>libstdc++-static</code> must be included in the AlmaLinux 9 build environment because <code>template_release</code> links libstdc++ statically.</li>
<li>The binary filename is <code>godot.linuxbsd.template_release.double.x86_64</code>.</li>
<li><code>--headless</code> flag is required at runtime.</li>
<li>Double precision matches the physics and networking precision of the rest of the stack.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260501-godot-double-precision-template-release-for-zone.html</guid>
  <pubDate>Fri, 01 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Fly.io for deployment</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260501-fly-io-for-deployment.html</link>
  <description><![CDATA[ 





<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p>The stack requires a platform that supports UDP (for QUIC/WebTransport), private networking between services, persistent volumes, and on-demand machines for job workloads (asset baking).</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Deploy all services on Fly.io in the <code>iad</code> (Ashburn, Virginia) region.</p>
<ul>
<li>Machines handle deployment; Terraform (fly-apps/fly provider v0.0.21) manages resources (apps, IPs, volumes, secrets) but not machines, due to Machines API limitations in the provider.</li>
<li>Private networking uses Fly’s 6PN (WireGuard mesh). All <code>.internal</code> DNS resolves to IPv6 only, requiring <code>socket_options: [:inet6]</code> in any Erlang/Elixir TCP stack.</li>
<li>Dedicated IPv4 is required for UDP. Fly passes UDP without DNAT; the app must bind to the same port clients connect to.</li>
<li>On-demand machines (Fly Machines API) are used for the baker — one machine per bake job, exits when done.</li>
</ul>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>All services must be in the same region to communicate over the private network without egress charges.</li>
<li><code>.flycast</code> requires <code>[[services]]</code> on the target app; <code>.internal</code> works without it.</li>
<li>Cloudflare proxy must be disabled for UDP/QUIC endpoints; DNS-only records point directly to the Fly IP.</li>
<li>Terraform state is stored on a <code>tfstate</code> orphan branch in the infra repo to avoid requiring a remote backend.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260501-fly-io-for-deployment.html</guid>
  <pubDate>Fri, 01 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>CockroachDB with mTLS and role-separated access</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260501-cockroachdb-with-mtls-role-separation.html</link>
  <description><![CDATA[ 





<section id="context" class="level2">
<h2 class="anchored" data-anchor-id="context">Context</h2>
<p>The stack needs a relational database reachable by the Elixir gateway and the Phoenix zone backend. It must support schema migrations (DDL) separately from application queries (DML) to limit blast radius if application credentials are compromised.</p>
</section>
<section id="decision" class="level2">
<h2 class="anchored" data-anchor-id="decision">Decision</h2>
<p>Run a single-node CockroachDB on Fly.io with mTLS for all connections and three application roles:</p>
<ul>
<li><code>gateway_admin</code> — DDL access for Ecto migrations</li>
<li><code>gateway_writer</code> — DML access for the running application</li>
<li><code>gateway_reader</code> — read-only access</li>
</ul>
<p>Certs are generated by <code>gen_crdb_certs.sh</code> with 100-year validity and stored as Fly secrets. The node cert must include both <code>serverAuth</code> and <code>clientAuth</code> in <code>extendedKeyUsage</code> because CockroachDB uses the node cert for internal gRPC client authentication.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li><code>--advertise-addr</code> must be <code>localhost</code>. A flycast address routes the internal gRPC loopback through Fly’s NAT, breaking the admin UI.</li>
<li><code>prepare: :unnamed</code> is required in Postgrex to avoid statement-cache OOM on single-node deployments.</li>
<li>Port 26257 is never publicly exposed. Access is via Fly’s private network (6PN) using <code>socket_options: [:inet6]</code> in Ecto, because <code>.internal</code> DNS returns only AAAA records.</li>
<li>The <code>root</code> cert is provisioned on the CRDB machine only. <code>gateway_admin</code> is the highest-privilege cert available to the application.</li>
</ul>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260501-cockroachdb-with-mtls-role-separation.html</guid>
  <pubDate>Fri, 01 May 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/present-proposal-template.html</link>
  <description><![CDATA[ 





<section id="proposal-title" class="level1">
<h1>[Proposal Title]</h1>
<section id="the-context" class="level2">
<h2 class="anchored" data-anchor-id="the-context">The Context</h2>
<p>Describe the circumstances that led to this proposal. Give enough background that someone unfamiliar with the area can understand why the work matters. State what you were trying to accomplish, not what solution you reached for first.</p>
</section>
<section id="the-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="the-problem-statement">The Problem Statement</h2>
<p>State the specific problem, limitation, or gap being addressed. Be concrete. A reader should be able to confirm whether a proposed solution actually solves it.</p>
</section>
<section id="design" class="level2">
<h2 class="anchored" data-anchor-id="design">Design</h2>
<p>Detail the proposed solution precisely enough that it could be implemented without improvising the design. Include code, diagrams, or examples where they reduce ambiguity. Vague or underspecified proposals cannot be acted on.</p>
</section>
<section id="cris-score" class="level2">
<h2 class="anchored" data-anchor-id="cris-score">CRIS Score</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 12%">
<col style="width: 4%">
<col style="width: 83%">
</colgroup>
<thead>
<tr class="header">
<th>Factor</th>
<th>Score</th>
<th>Evidence</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>C</strong>omplexity</td>
<td>0–10</td>
<td>[10 = simple, well-understood; 0 = novel or high-risk — why]</td>
</tr>
<tr class="even">
<td><strong>R</strong>each</td>
<td>0–10</td>
<td>[10 = affects everyone; 0 = affects almost no one — why]</td>
</tr>
<tr class="odd">
<td><strong>I</strong>mpediment</td>
<td>0–10</td>
<td>[10 = total blocker with no workaround; 0 = trivial to work around — why]</td>
</tr>
<tr class="even">
<td><strong>S</strong>takeholder</td>
<td>0–10</td>
<td>[10 = critical dependency for a major project; 0 = no project depends on it — why]</td>
</tr>
<tr class="odd">
<td><strong>Total</strong></td>
<td></td>
<td>[Average of four factors (0–10); 8–10 = build now, 6–8 = schedule soon, 4–6 = evaluate, 0–4 = defer]</td>
</tr>
</tbody>
</table>
</section>
<section id="the-downsides" class="level2">
<h2 class="anchored" data-anchor-id="the-downsides">The Downsides</h2>
<p>Describe the costs, risks, or trade-offs. Every proposal has them.</p>
</section>
<section id="the-road-not-taken" class="level2">
<h2 class="anchored" data-anchor-id="the-road-not-taken">The Road Not Taken</h2>
<p>Describe the alternatives considered and why this approach was chosen over them.</p>
</section>
<section id="status" class="level2">
<h2 class="anchored" data-anchor-id="status">Status</h2>
<p>Status: Draft <!-- Draft | Proposed | Rejected | Accepted | Deprecated | Superseded by [Link/Reference] --></p>
</section>
<section id="decision-makers" class="level2">
<h2 class="anchored" data-anchor-id="decision-makers">Decision Makers</h2>
<ul>
<li>[Name or role]</li>
</ul>
</section>
<section id="tags" class="level2">
<h2 class="anchored" data-anchor-id="tags">Tags</h2>
<ul>
<li>[relevant-tag-1], [relevant-tag-2], YYYYMMDD-this-filename-without-extension, present-proposal-template</li>
</ul>
<!-- Always include the filename (without .md) as the last tag.
     This makes the document findable by filename from any grep or
     code-search tool, the same way Godot Engine source files are
     cross-referenced by their basename. -->
</section>
<section id="further-reading" class="level2">
<h2 class="anchored" data-anchor-id="further-reading">Further Reading</h2>
<!-- Written in Bibtex format -->
<pre><code>@misc{v_sekai_2026,
  title = {V-Sekai},
  year  = {2026},
  url   = {https://v-sekai.org/}
}</code></pre>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/present-proposal-template.html</guid>
  <pubDate>Sat, 06 Jun 2026 16:10:22 GMT</pubDate>
</item>
<item>
  <title></title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260506-maglev-intercept-smoke-test.html</link>
  <description><![CDATA[ 





<section id="maglev-intercept-cross-platform-tilt-shift-smoke-test" class="level1">
<h1>Maglev Intercept: Cross-Platform Tilt-Shift Smoke Test</h1>
<section id="the-context" class="level2">
<h2 class="anchored" data-anchor-id="the-context">The Context</h2>
<p>The stack: <code>multiplayer-fabric-gateway</code> (Elixir, UDP 443 → zone UDP 7443, WebTransport/QUIC datagrams), <code>multiplayer-fabric-zone</code> (headless Godot <code>template_release double</code>), <code>multiplayer-fabric-uro</code> (Phoenix shard registry and asset API), single-node CockroachDB on Fly’s 6PN private network.</p>
<p>Zone authority is geometric — <code>ZoneRange.contains(hilbert3D(pos))</code> determines ownership, gossip-learned, with uniqueness proven under <code>DisjointRanges</code>. Scores and persistent player state sit in a reserved logical partition of the 30-bit Hilbert space (the persona partition), governed by the same invariant as spatial zones. This is the first attempt to load all four layers together under cross-platform input.</p>
</section>
<section id="the-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="the-problem-statement">The Problem Statement</h2>
<p>The full scenario — cross-platform clients, authoritative rolling-core physics, and a causal cross-zone score commit — is too complex to build and validate in one pass. A system assembled from untested parts at full complexity has no debuggable baseline.</p>
</section>
<section id="design" class="level2">
<h2 class="anchored" data-anchor-id="design">Design</h2>
<p>Players start mid-breach on a moving Maglev Data-Courier train, fight drones, retrieve Quantum Data-Cores rolling across the banking floor, and slot them into the mainframe terminal within 3 minutes. The PCVR client sees the car as a waist-height diorama; the Steam Deck client sees the same instance as an isometric action-RPG.</p>
<p>Each layer is a separate cycle — a working system and a gate before the next begins.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Cycle</th>
<th>C</th>
<th>What it proves</th>
<th>Depends on</th>
<th>Document</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>0</td>
<td>8</td>
<td>Terraform applies Fly.io resources; all apps, IPs, volumes, and secrets present</td>
<td>—</td>
<td><a href="../decisions/20260506-maglev-cycle-0-infra.html">cycle-0</a></td>
</tr>
<tr class="even">
<td>1</td>
<td>6</td>
<td>Gateway + zone handshake; one Godot datagram end-to-end</td>
<td>0</td>
<td><a href="../decisions/20260506-maglev-cycle-1-gateway-handshake.html">cycle-1</a></td>
</tr>
<tr class="odd">
<td>2</td>
<td>7</td>
<td>OTEL → VictoriaTraces/Metrics/Logs live (parallel)</td>
<td>1</td>
<td><a href="../decisions/20260506-maglev-cycle-2-observability.html">cycle-2</a></td>
</tr>
<tr class="even">
<td>3</td>
<td>7</td>
<td>Baker: train scene + VRM avatars via aria-storage → uro (parallel)</td>
<td>1</td>
<td><a href="../decisions/20260506-maglev-cycle-3-baker.html">cycle-3</a></td>
</tr>
<tr class="odd">
<td>4</td>
<td>8</td>
<td>Direct uro → CockroachDB connection; mTLS, IPv6, prepare:unnamed (parallel)</td>
<td>1</td>
<td><a href="../decisions/20260506-maglev-cycle-4-db-connection.html">cycle-4</a></td>
</tr>
<tr class="even">
<td>5</td>
<td>7</td>
<td>Zone-console (ratatui TUI) connects and shows tick rate (parallel)</td>
<td>1</td>
<td><a href="../decisions/20260506-maglev-cycle-5-zone-console.html">cycle-5</a></td>
</tr>
<tr class="odd">
<td>6</td>
<td>6</td>
<td>16 clients (14 taskweft bots + PCVR + Steam Deck); broadcast at MMOG load</td>
<td>1, 2</td>
<td><a href="../decisions/20260506-maglev-cycle-6-mmog-load.html">cycle-6</a></td>
</tr>
<tr class="even">
<td>7</td>
<td>7</td>
<td>IK routing and merge: format proven at 1 Hz, merge logic at 10 Hz</td>
<td>6</td>
<td><a href="../decisions/20260506-maglev-cycle-7-ik-routing.html">cycle-7</a></td>
</tr>
<tr class="odd">
<td>8</td>
<td>6</td>
<td>Full headset-rate IK merged without head-of-line blocking</td>
<td>7</td>
<td><a href="../decisions/20260506-maglev-cycle-8-ik-merge.html">cycle-8</a></td>
</tr>
<tr class="even">
<td>9</td>
<td>6</td>
<td>Banking train + stationary cores replicate; VRM avatars load from chunk store</td>
<td>8, 3</td>
<td><a href="../decisions/20260506-maglev-cycle-9-physics-vrm.html">cycle-9</a></td>
</tr>
<tr class="odd">
<td>10</td>
<td>6</td>
<td>Rolling cores under banking + causal ordering of core-slot events</td>
<td>9, 2, 5</td>
<td><a href="../decisions/20260506-maglev-cycle-10-dynamic-physics-score.html">cycle-10</a></td>
</tr>
<tr class="even">
<td>11</td>
<td>6</td>
<td>Full causal write: persona zone commits; uro persists score</td>
<td>10, 4</td>
<td><a href="../decisions/20260506-maglev-cycle-11-db-write.html">cycle-11</a></td>
</tr>
<tr class="odd">
<td>12</td>
<td>3</td>
<td>Game cycle planning: scoped ADRs and estimates for drone AI, combat, grab, slot, timer, camera modes</td>
<td>9</td>
<td><a href="../decisions/20260506-maglev-cycle-12-game-planning.html">cycle-12</a></td>
</tr>
</tbody>
</table>
<p>Taskweft-confirmed ordering: 13 cycles (0–12) running in number order. Cycle 0 gates everything. Parallel tracks 2–5 all start after Cycle 1; Cycle 2 (observability) must complete before Cycle 6; Cycle 3 (baker) must complete before Cycle 9; Cycle 5 (zone-console) must complete before Cycle 10; bake jobs (1–3 h) always complete before the networking cycles (≥1 day each).</p>
<p>Cycle 7 combines 1 Hz routing and 10 Hz merge into one cycle; Cycle 8 adds full headset rate. If the team is confident in the merge after Cycle 7, Cycle 8 can be skipped.</p>
<p><code>multiplayer-fabric-predictive-bvh</code> (interest management via predictive BVH) is exercised in Cycle 10: with 16 moving clients the BVH computes at least 2 distinct interest zones, verified in zone-console. Full cross-zone interest management (multiple zone servers) is left for a separate cycle series.</p>
</section>
<section id="cris-score" class="level2">
<h2 class="anchored" data-anchor-id="cris-score">CRIS Score</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 9%">
<col style="width: 3%">
<col style="width: 87%">
</colgroup>
<thead>
<tr class="header">
<th>Factor</th>
<th>Score</th>
<th>Evidence</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>C</strong>omplexity</td>
<td>4</td>
<td>Merging a 6-DOF IK stream with gamepad input in one physics tick, plus a cross-zone causal score commit, is untested in this codebase.</td>
</tr>
<tr class="even">
<td><strong>R</strong>each</td>
<td>10</td>
<td>Every future gameplay module runs on the gateway, zone server, replication cycle, and persona zone these cycles exercise.</td>
</tr>
<tr class="odd">
<td><strong>I</strong>mpediment</td>
<td>9</td>
<td>Physics desync or a broken persona write path blocks game design; either requires rewriting the netcode layer.</td>
</tr>
<tr class="even">
<td><strong>S</strong>takeholder</td>
<td>10</td>
<td>Primary proof-of-concept for the V-Sekai Fire multiplayer fabric.</td>
</tr>
<tr class="odd">
<td><strong>Total</strong></td>
<td>8.25</td>
<td>Build now.</td>
</tr>
</tbody>
</table>
</section>
<section id="the-downsides" class="level2">
<h2 class="anchored" data-anchor-id="the-downsides">The Downsides</h2>
<p>Cycle 10 requires Cyberprep art assets and MToon shaders tuned for both targets before it can run. Physics replication will stress WebTransport bandwidth. If the persona zone goes down, that player’s attributes are offline until it recovers. The shared-world UX for a standing VR player and a seated thumbstick player needs a separate design pass that cannot begin until Cycle 10 is stable.</p>
<p>Three gaps against industry par remain out of scope for this smoke test. Network condition simulation (artificial latency, jitter, packet loss) is standard in Unity Netcode, Photon, and Mirror but is not injected in these cycles. Reconnect storm testing — all 16 clients disconnecting and reconnecting simultaneously — is absent; VRChat’s largest outage traced to this failure mode. Automated CI/CD execution is also absent; industry runs a 5-minute smoke gate on every deploy, whereas these cycles are human-run, week-scale exercises.</p>
</section>
<section id="the-road-not-taken" class="level2">
<h2 class="anchored" data-anchor-id="the-road-not-taken">The Road Not Taken</h2>
<p>A steampunk skyship setting was rejected — it conflicts with the anime/VRM art targets and drifts toward fantasy MMO conventions.</p>
<p>Building the full scenario in one pass was the original plan, dropped when the staging approach proved easier to attribute failures.</p>
</section>
<section id="status" class="level2">
<h2 class="anchored" data-anchor-id="status">Status</h2>
<p>Status: Draft</p>
</section>
<section id="decision-makers" class="level2">
<h2 class="anchored" data-anchor-id="decision-makers">Decision Makers</h2>
<ul>
<li>Lead Architect / Fabric Maintainer</li>
<li>Game Director</li>
</ul>
</section>
<section id="tags" class="level2">
<h2 class="anchored" data-anchor-id="tags">Tags</h2>
<ul>
<li>smoke-test, cross-platform, tilt-shift, cyberprep, in-medias-res, persona-zone, shared-nothing, galls-law, 20260506-maglev-intercept-smoke-test, present-proposal-template</li>
</ul>
</section>
<section id="further-reading" class="level2">
<h2 class="anchored" data-anchor-id="further-reading">Further Reading</h2>
<pre><code>@misc{gall_1975,
  author = {Gall, John},
  title  = {Systemantics: How Systems Work and Especially How They Fail},
  year   = {1975}
}

@misc{v_sekai_2026,
  title = {V-Sekai},
  year  = {2026},
  url   = {https://v-sekai.org/}
}</code></pre>


</section>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260506-maglev-intercept-smoke-test.html</guid>
  <pubDate>Sat, 06 Jun 2026 16:10:22 GMT</pubDate>
</item>
</channel>
</rss>
